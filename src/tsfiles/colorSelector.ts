export default class colorSelector {
    blur: number;
    opacity: number;
    h: number;
    s: number;
    v: number;

    constructor(blur: number, opacity: number, h: number, s: number, v: number) {
        this.blur = blur;
        this.opacity = opacity;
        this.h = h;
        this.s = s;
        this.v = v;

    }
}